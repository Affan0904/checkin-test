/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "rent-clone-two-lilac.vercel.app",
      "wondercrafts.org",
      "images.unsplash.com",
      "unsplash.com",
    ],
  },
};

module.exports = nextConfig;
